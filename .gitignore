# Binaries
/sharko
bin/
*.exe
_dist/

# Go
vendor/

# Node
ui/node_modules/
ui/dist/
scripts/node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Secrets
secrets.env
.env
.env.*
*.pem
*.key

# AI workspaces (local config, not project conventions)
# .claude/ itself is NOT blanket-ignored: skills, team role files, hooks, and
# settings.json are project conventions and are tracked normally. Only the
# per-session / per-machine state under .claude/ is ignored below.
.claude/checkpoints/
.claude/worktrees/
.claude/settings.local.json
.serena/
.cursor/
.copilot/
.continue/
.aider*
.planning/
_bmad/
.bmad/
# CLAUDE.md is intentionally tracked — it contains project conventions for AI sessions

# Docker
aap-server

# Test
coverage.out
coverage/
*.test

# Build
*.tar.gz
.superpowers/
# Stray local binaries/caches that sometimes land at repo root
/playground
__pycache__/

# MkDocs build output
/site/

# Internal planning docs (not for public repo)
docs/superpowers/

# Local maintainer scratch (kind-target kubeconfigs with bearer tokens, etc.)
.local/

# Go build/test caches (local only)
.gocache/
.gotmp/

# Agent worktree dirs (used by code-agent dispatch)
.claire/
.clone/

.workflow-playbook/
