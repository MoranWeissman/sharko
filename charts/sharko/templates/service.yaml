apiVersion: v1
kind: Service
metadata:
  name: {{ include "sharko.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sharko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sharko.selectorLabels" . | nindent 4 }}
