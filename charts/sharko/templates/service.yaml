apiVersion: v1
kind: Service
metadata:
  name: {{ include "aap.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aap.selectorLabels" . | nindent 4 }}
