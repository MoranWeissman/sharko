---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clusteraddons.sharko.dev
spec:
  group: sharko.dev
  names:
    kind: ClusterAddons
    listKind: ClusterAddonsList
    plural: clusteraddons
    shortNames:
    - ca
    singular: clusteraddons
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.cluster
      name: Cluster
      type: string
    - jsonPath: .status.syncedAddons
      name: Synced
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ClusterAddons is the Schema for the clusteraddons API.
          It defines a namespaced custom resource that represents the addon inventory
          for a single managed cluster. In Phase 1, the Sharko operator watches ClusterAddons
          objects and writes a read-only .status projection of the cluster's addon-reconcile
          outcome. The CRD does not drive addon deployments in Phase 1; a future phase will
          invert this so the CR spec becomes the source of truth.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ClusterAddonsSpec defines the desired addon assignments for a managed cluster.
              This is the ASSIGNMENT layer only — it specifies which addons are enabled/disabled
              and their versions. The addon catalog holds the chart definitions, Helm repos, and
              default values. Separating assignment from definition avoids duplication and keeps
              the catalog as the single source of truth for addon metadata.
            properties:
              addons:
                description: |-
                  Addons is the list of addon assignments for this cluster.
                  Each entry specifies an addon name, optional version override, and enabled state.
                items:
                  description: |-
                    AddonAssignment represents a single addon's configuration for a cluster.
                    The Name field is the only required field; Version and Enabled are optional overrides.
                  properties:
                    enabled:
                      description: |-
                        Enabled controls whether this addon is active on the cluster.
                        If nil, defaults to true (addon is enabled).
                      type: boolean
                    name:
                      description: Name of the addon (must exist in the addon catalog).
                      minLength: 1
                      type: string
                    version:
                      description: |-
                        Version is an optional version override for this cluster.
                        If not specified, the cluster uses the global version from the catalog.
                      type: string
                  required:
                  - name
                  type: object
                type: array
              cluster:
                description: |-
                  Cluster is the name of the managed cluster this assignment applies to.
                  Must match a cluster name in the managed-clusters.yaml file.
                minLength: 1
                type: string
            required:
            - cluster
            type: object
          status:
            description: |-
              ClusterAddonsStatus defines the observed state of ClusterAddons.
              It tracks the reconciliation outcome and provides status for kubectl get commands.
            properties:
              conditions:
                description: |-
                  Conditions represent the latest available observations of the ClusterAddons state.
                  Standard condition types: Ready, Progressing, Degraded.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of the most recent
                  reconciliation attempt.
                format: date-time
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed by the reconciler.
                  It corresponds to the ClusterAddons' metadata.generation, which is updated on
                  any changes to the spec.
                format: int64
                type: integer
              syncedAddons:
                description: SyncedAddons is the count of addons successfully reconciled
                  in the last run.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
