# Example per-cluster values file
# Generated by: sharko add-cluster <name> --addons cert-manager,external-secrets
# Each addon section overrides the global values for this specific cluster.
#
# clusterGlobalValues:
#   env: dev
#   clusterName: my-cluster
#   region: eu-central-1
#
# cert-manager:
#   replicaCount: 3
#
# datadog:
#   datadog:
#     clusterName: my-cluster
