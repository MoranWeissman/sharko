# Helm values for cert-manager — applied to all clusters
# Top-level keys are passed directly to the cert-manager Helm chart.
# Docs: https://cert-manager.io/docs/configuration/

# installCRDs: true
# replicaCount: 2
