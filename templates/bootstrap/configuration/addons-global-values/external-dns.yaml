# Global Helm values for ExternalDNS
# Docs: https://kubernetes-sigs.github.io/external-dns/latest/
external-dns:
  enabled: false
  # provider: aws              # aws, cloudflare, azure, google
  # domainFilters:
  #   - example.com
  # policy: sync               # sync = create+delete, upsert-only = create only
  # serviceAccount:
  #   annotations:
  #     eks.amazonaws.com/role-arn: arn:aws:iam::role/external-dns
