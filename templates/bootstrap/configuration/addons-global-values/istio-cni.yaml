# Global Helm values for istio-cni
# Docs: https://istio.io/latest/docs/setup/install/helm/
# Deploy order: istio-base (syncWave -2) → istio-cni (-1) → istiod (0) → istio-ingress (1)
istio-cni:
  enabled: false
  # cni:
  #   cniBinDir: /opt/cni/bin
