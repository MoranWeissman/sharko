# Bootstrap configuration — generated by sharko init
# These values are used by the ArgoCD root Application to locate this repository.

repoURL: 'SHARKO_GIT_REPO_URL'
targetRevision: SHARKO_GIT_BRANCH

# Host cluster (the cluster running ArgoCD + Sharko)
hostCluster:
  name: SHARKO_HOST_CLUSTER_NAME
