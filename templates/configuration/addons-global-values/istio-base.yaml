# ====================================================================== #
# Istio Base (CRDs) Global Values
# ====================================================================== #
# Chart: base
# Repo: https://istio-release.storage.googleapis.com/charts
# Namespace: istio-system
# Docs: https://istio.io/latest/docs/setup/install/helm/
#
# These values apply to ALL clusters unless overridden per-cluster
# in addons-clusters-values/<cluster-name>.yaml
#
# NOTE: This chart only installs Istio CRDs. It has minimal config.
# ====================================================================== #

# ---- Default Revision ---- #
# Set to match the istiod revision if using canary upgrades
# defaultRevision: ""

# ---- Global Settings ---- #
# global:
#   istioNamespace: istio-system

# ====================================================================== #
# Cluster-Specific Overrides Reference
# ====================================================================== #
# Copy the relevant sections below into your cluster values file:
#   addons-clusters-values/<cluster-name>.yaml
#
# istio-base:
#   # Override default revision per cluster (for canary upgrades)
#   defaultRevision: "1-28-0"
