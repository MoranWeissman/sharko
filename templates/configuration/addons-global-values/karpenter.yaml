# ====================================================================== #
# Karpenter CRDs Global Values
# ====================================================================== #
# Chart: karpenter-crds-helm-chart
# Repo: https://aic.jfrog.io/artifactory/aic-helm-charts/
# Docs: https://karpenter.sh/docs/
#
# These values apply to ALL clusters unless overridden per-cluster
# in addons-clusters-values/<cluster-name>.yaml
#
# NOTE: This is a CRDs-only chart (custom internal chart from JFrog).
# It installs Karpenter CRDs (NodePool, EC2NodeClass, etc.) without
# deploying the Karpenter controller itself.
# The controller is managed separately (e.g., by EKS Auto Mode or
# a dedicated Karpenter deployment).
# ====================================================================== #

# This chart has minimal configuration since it only installs CRDs.
# Values depend on the specific internal chart structure.
# Refer to the chart's README in JFrog for available options.

# ====================================================================== #
# Cluster-Specific Overrides Reference
# ====================================================================== #
# This CRDs-only chart typically doesn't need per-cluster overrides.
# If the internal chart supports any config, set it here:
#
# karpenter:
#   # (check chart README for available values)
