# ---- Argo Events EKS Auto Mode infra-karpenter Overrides ---- #
#
# Components and their scheduling:
#   controller    → infrastructure nodes (nodeSelector + toleration)
#

# Events Controller
controller:
  nodeSelector:
    node-type: infrastructure
  tolerations:
    - key: infrastructure
      operator: Equal
      value: "true"
      effect: NoSchedule
