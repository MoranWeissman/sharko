# ---- External DNS EKS Auto Mode infra-karpenter Overrides ---- #
#
# Components and their scheduling:
#   controller    → infrastructure nodes (nodeSelector + toleration)
#

# External DNS Controller
nodeSelector:
  node-type: infrastructure
tolerations:
  - key: infrastructure
    operator: Equal
    value: "true"
    effect: NoSchedule
