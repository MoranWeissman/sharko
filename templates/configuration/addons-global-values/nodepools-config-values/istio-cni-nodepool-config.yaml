# ---- Istio EKS Auto Mode infra-karpenter Overrides ---- #

# Istio CNI - target infrastructure nodes
cni:
  nodeSelector:
    node-type: infrastructure
  tolerations:
    - key: infrastructure
      operator: Equal
      value: "true"
      effect: NoSchedule
