# ---- RedisInsight EKS Auto Mode infra-karpenter Overrides ---- #
#
# Components and their scheduling:
#   app    → infrastructure nodes (nodeSelector + toleration)
#

# RedisInsight
nodeSelector:
  node-type: infrastructure
tolerations:
  - key: infrastructure
    operator: Equal
    value: "true"
    effect: NoSchedule
