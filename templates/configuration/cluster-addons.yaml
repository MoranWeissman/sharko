# ================================================================ #
# Clusters and Addons Management
# ================================================================ #
# Add or remove clusters and assign addons via labels
#
# Structure:
#   - name: cluster-name              # Required: EKS cluster name
#     labels:                         # Required: addon assignments
#       <addon-name>: enabled         # Enable addon with default version
#       <addon-name>-version: "X.Y.Z" # Override addon version
#
# Note: AWS Secrets Manager secret name is automatically prefixed with "k8s-"
#       Example: cluster name "my-app-dev" -> secret "k8s-my-app-dev"
#
# Example cluster configuration
# - name: cluster-name
#   labels:
#     datadog: enabled
#     datadog-version: "3.70.7"
#
# See docs/VALUES_GUIDE.md for detailed documentation
# ================================================================ #

clusters:

  # ================================================================ #
  # Development Clusters
  # ================================================================ #

  - name: cluster-dev-eks
    labels:
      hello-world: enabled
    #   datadog: enabled
    #   datadog-version: "3.70.7"
    #   anodot: enabled
    #   keda: enabled

  - name: cluster-staging-eks
    labels:
      hello-world: enabled
    #   datadog: enabled
    #   external-secrets: enabled

  # ================================================================ #
  # Example: Full addon configuration
  # ================================================================ #

  # - name: my-app-dev-eks
  #   labels:
  #     hello-world: enabled
  #     datadog: enabled
  #     datadog-version: "3.70.7"
  #     keda: enabled
  #     anodot: enabled
  #     external-secrets: enabled
  #     cert-manager: enabled
  #     argo-rollouts: enabled
  #     istio-base: enabled
  #     istiod: enabled
  #     istio-cni: enabled
  #     istio-ingress: enabled
