{
  "name": "Addon CPU near limits in {{kube_namespace.name}}",
  "type": "query alert",
  "query": "avg(last_15m):( avg:kubernetes.cpu.usage.total{kube_cluster_name:*,kube_namespace IN (datadog,keda,external-secrets)} by {kube_namespace} / avg:kubernetes.cpu.limits{kube_cluster_name:*,kube_namespace IN (datadog,keda,external-secrets)} by {kube_namespace} ) * 100 > 90",
  "message": "Addon in namespace {{kube_namespace.name}} on cluster $cluster_name is using more than 90% of its CPU limit for 15 minutes.\n\n    Consider increasing CPU limits or investigating high CPU usage.\n    Check the Addon Deployment Health dashboard \u2192 Resource Usage section.",
  "tags": [
    "cluster:$cluster_name",
    "source:addon-monitoring",
    "severity:warning"
  ],
  "options": {
    "thresholds": {
      "critical": "90",
      "warning": "80"
    }
  }
}
