{
  "name": "K8s error events in addon namespace {{kube_namespace.name}}",
  "type": "event-v2 alert",
  "query": "events(\\",
  "message": "More than 5 Kubernetes error events in namespace {{kube_namespace.name}} on cluster $cluster_name in the last 10 minutes.\n\n    Check the Addon Deployment Health dashboard \u2192 Kubernetes Events section for details.",
  "tags": [
    "cluster:$cluster_name",
    "source:addon-monitoring",
    "severity:warning"
  ],
  "options": {
    "thresholds": {
      "critical": "5"
    }
  }
}
