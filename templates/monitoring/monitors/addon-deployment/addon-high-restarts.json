{
  "name": "Addon high restart rate in {{kube_namespace.name}}",
  "type": "query alert",
  "query": "change(avg(last_10m),last_10m):max:kubernetes_state.container.restarts{kube_cluster_name:*,kube_namespace IN (datadog,keda,external-secrets)} by {kube_namespace} > 5",
  "message": "Containers in namespace {{kube_namespace.name}} on cluster $cluster_name have restarted more than 5 times in the last 10 minutes.\n\n    Check the Addon Deployment Health dashboard \u2192 Pod Restarts Over Time for details.",
  "tags": [
    "cluster:$cluster_name",
    "source:addon-monitoring",
    "severity:warning"
  ],
  "options": {
    "thresholds": {
      "critical": "5",
      "warning": "3"
    }
  }
}
