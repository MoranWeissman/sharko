# cert-manager — default Helm values
# These are applied to all clusters. Per-cluster overrides go in
# configuration/addons-clusters-values/<cluster-name>.yaml

installCRDs: true

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    memory: 128Mi
