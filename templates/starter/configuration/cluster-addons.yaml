# Cluster Registry — clusters managed by Sharko
#
# Each cluster has labels that control which addons are deployed.
# Set "<addon>: enabled" to deploy an addon to a cluster.
# Set "<addon>: disabled" or remove the label to skip it.
#
# Clusters are registered via 'sharko add-cluster' or the API.
# This file is managed by Sharko — manual edits are preserved.

clusters: []
  # Example:
  # - name: prod-eu
  #   labels:
  #     cert-manager: enabled
  #     metrics-server: enabled
  #     region: eu-west-1
