# Regression fixture for V2-cleanup-17 — an ENVELOPED managed-clusters file.
# The cluster list lives under spec.clusters (sharko.dev/v1 envelope).
apiVersion: sharko.dev/v1
kind: ManagedClusters
metadata:
  name: managed-clusters
spec:
  clusters:
    - name: cluster-test-3
      region: eu-central-1
      labels:
        velero: enabled
