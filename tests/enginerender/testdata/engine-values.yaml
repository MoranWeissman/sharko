# Fixture engine-values.yaml — stands in for what production sets via
# `helm.parameters` on the engine pin, sharko-engine.yaml (design doc section 2.5).
# No curated addon data here any more — decision 6 of the
# 2026-07-31-catalog-approved-model design doc removed the chart's own baked
# curated block entirely. catalog.yaml (this fixture directory's own
# catalog.yaml) is the ONLY source of addon definitions now, and it carries
# full entries (repoURL/chart/namespace/version) on its own.
repo:
  url: https://github.com/example-org/fleet-gitops.git
  revision: main
hostCluster:
  name: hub
project:
  name: sharko-addons
